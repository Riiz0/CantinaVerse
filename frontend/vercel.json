{
  "version": 2,
  "builds": [
    {
      "src": "/package.json",
      "use": "@vercel/static-build",
      "config": { 
        "install_command": "npm install",
        "build_command": "npm run build"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/.next/$1" }
  ]
}
